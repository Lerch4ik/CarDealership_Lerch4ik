<Window x:Class="CarDealership.Views.Dialogs.CustomerEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Клиент" Width="560" Height="520" MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BackgroundDarkBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTitle" Text="Добавить клиента" FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,25"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Фамилия *" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtLastName" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Имя *" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtFirstName" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Отчество" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtMiddleName" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Телефон" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtPhone" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Email" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtEmail" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Паспорт" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtPassport" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="3" Margin="0,0,0,15">
                    <TextBlock Text="Адрес" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtAddress" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="4" Margin="0,0,0,15">
                    <TextBlock Text="Заметки" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtNotes" Style="{DynamicResource ModernTextBox}"
                             Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Отмена" Padding="20,10" Margin="0,0,10,0"
                    Click="BtnCancel_Click" Background="{DynamicResource BackgroundCardBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave" Content="Сохранить" Padding="25,10"
                    Style="{DynamicResource PrimaryButton}" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
