<Window x:Class="CarDealership.Views.Dialogs.ServiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сервисная заявка" Width="560" Height="490" MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BackgroundDarkBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Новая сервисная заявка" FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,25"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Автомобиль *" Style="{DynamicResource FormLabel}"/>
                    <ComboBox x:Name="CmbCar" Style="{DynamicResource ModernComboBox}" DisplayMemberPath="DisplayName"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Клиент" Style="{DynamicResource FormLabel}"/>
                    <ComboBox x:Name="CmbCustomer" Style="{DynamicResource ModernComboBox}" DisplayMemberPath="FullName"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Тип работ" Style="{DynamicResource FormLabel}"/>
                    <ComboBox x:Name="CmbType" Style="{DynamicResource ModernComboBox}">
                        <ComboBoxItem Content="ТО" IsSelected="True"/>
                        <ComboBoxItem Content="Ремонт"/>
                        <ComboBoxItem Content="Диагностика"/>
                        <ComboBoxItem Content="Кузовной ремонт"/>
                        <ComboBoxItem Content="Шиномонтаж"/>
                        <ComboBoxItem Content="Детейлинг"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Стоимость (₽)" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtCost" Text="0" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="3" Margin="0,0,0,15">
                    <TextBlock Text="Описание работ" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtDesc" Style="{DynamicResource ModernTextBox}"
                             Height="65" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
                <StackPanel Grid.Column="0" Grid.Row="4" Margin="0,0,0,15">
                    <TextBlock Text="Техник" Style="{DynamicResource FormLabel}"/>
                    <TextBox x:Name="TxtTech" Style="{DynamicResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="4" Margin="0,0,0,15">
                    <TextBlock Text="Статус" Style="{DynamicResource FormLabel}"/>
                    <ComboBox x:Name="CmbStatus" Style="{DynamicResource ModernComboBox}">
                        <ComboBoxItem Content="In Progress" IsSelected="True"/>
                        <ComboBoxItem Content="Completed"/>
                        <ComboBoxItem Content="Cancelled"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Отмена" Padding="20,10" Margin="0,0,10,0"
                    Click="BtnCancel_Click" Background="{DynamicResource BackgroundCardBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Создать заявку" Padding="25,10"
                    Style="{DynamicResource PrimaryButton}" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
