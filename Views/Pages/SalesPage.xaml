<Page x:Class="CarDealership.Views.Pages.SalesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BackgroundDarkBrush}">

    <Grid Margin="30,25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Продажи" FontSize="26" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock x:Name="TxtCount" FontSize="14"
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="+ Новая продажа"
                    Style="{DynamicResource SuccessButton}" Click="BtnAdd_Click"/>
        </Grid>

        <!-- KPIs -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{DynamicResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Общая выручка" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtTotalRevenue" Text="₽ 0" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource AccentGreenBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{DynamicResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Продаж всего" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtTotalSales" Text="0" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="4" Style="{DynamicResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Ср. стоимость сделки" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtAvgSale" Text="₽ 0" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource AccentOrangeBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Search -->
        <Border Grid.Row="2" Style="{DynamicResource CardBorder}" Margin="0,0,0,15">
            <TextBox x:Name="TxtSearch" Style="{DynamicResource ModernTextBox}"
                     TextChanged="TxtSearch_TextChanged"/>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="3" Style="{DynamicResource CardBorder}">
            <DataGrid x:Name="GridSales" Style="{DynamicResource ModernDataGrid}"
                      MouseDoubleClick="GridSales_MouseDoubleClick"
                      CanUserResizeColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="45" MinWidth="40"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding SaleDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="95" MinWidth="85"/>
                    <DataGridTextColumn Header="Автомобиль" Binding="{Binding CarName}" Width="2*" MinWidth="130"/>
                    <DataGridTextColumn Header="Клиент" Binding="{Binding CustomerName}" Width="2*" MinWidth="120"/>
                    <DataGridTextColumn Header="Менеджер" Binding="{Binding ManagerName}" Width="*" MinWidth="100"/>
                    <DataGridTextColumn Header="Оплата" Binding="{Binding PaymentMethod}" Width="*" MinWidth="80"/>
                    <DataGridTextColumn Header="Скидка" Binding="{Binding Discount, StringFormat=\{0:N0\} ₽}" Width="*" MinWidth="80"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding SalePrice, StringFormat=\{0:N0\} ₽}" Width="*" MinWidth="100"/>
                    <DataGridTemplateColumn Header="Статус" Width="105" MinWidth="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="6" Padding="8,3" HorizontalAlignment="Left" Margin="0,3">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                    <Setter Property="Background" Value="{DynamicResource BadgeGreenBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                    <Setter Property="Background" Value="{DynamicResource BadgeOrangeBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                    <Setter Property="Background" Value="{DynamicResource BadgeRedBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                        <Setter Property="Text" Value="Завершена"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource AccentGreenBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                        <Setter Property="Text" Value="Ожидание"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource AccentOrangeBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                        <Setter Property="Text" Value="Отменена"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource AccentRedBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Действия" Width="105" MinWidth="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" Padding="8,4" FontSize="11"
                                        Style="{DynamicResource DangerButton}" Margin="0,4"
                                        Tag="{Binding Id}" Click="BtnDelete_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
